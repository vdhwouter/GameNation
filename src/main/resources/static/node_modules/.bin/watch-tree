#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../watch-tree-maintained/bin/watch-tree" "$@"
  ret=$?
else 
  node  "$basedir/../watch-tree-maintained/bin/watch-tree" "$@"
  ret=$?
fi
exit $ret
